const VERDICTS = ['선고유예', '벌금', '집행유예', '징역', '무기징역', '사형', '무죄'];

export default function VerdictPanel({ onSelect }) {
  return (
    <div className="verdict-grid">
      {VERDICTS.map((v) => (
        <button key={v} onClick={() => onSelect(v)}>{v}</button>
      ))}
    </div>
  );
}
