# 미니법정 기획 노트

이 문서는 "왜 이렇게 설계했는가"를 남기기 위한 기획 메모입니다.
포트폴리오나 지원서에 프로젝트를 설명할 때 이 문서를 근거로 스토리를 만들 수 있습니다.

## 1. 핵심 재미 — "적정함"이라는 긴장감

대부분의 재판/변호사 게임은 "무죄를 받아내라" 혹은 "범인을 찾아라" 같은 단일 목표를 가집니다.
미니법정은 반대로 **정답이 하나가 아니라 "범위"**라는 점을 게임의 핵심 긴장감으로 삼았습니다.

- 너무 가혹해도 안 되고 (법왜곡죄 - 과잉처벌)
- 너무 관대해도 안 되고 (법왜곡죄 - 솜방망이 / 시민의심 누적)
- 죄가 없는데 유죄를 선고해도 안 됨 (법왜곡죄 - 오판)

이 세 가지 실패 조건을 만들어서, 플레이어가 매 사건마다 "안전한 정답"을 찾는 게 아니라
**증거와 정황을 근거로 스스로 판단**하게 만드는 것이 목표였습니다.

## 2. 판정 로직 설계

각 사건은 다음 값을 가집니다.

| 필드 | 의미 |
|---|---|
| `guilty` | 실제로 유죄인지 (플레이어에게는 숨겨진 정답) |
| `fairMin` / `fairMax` | 이 범위 안의 형량이면 "적정 판결" |
| `outrageHigh` / `outrageLow` | 이 값을 넘으면 즉시 "법왜곡죄" |
| `disallowed` | 해당 죄목의 법정형에 없는 형벌 (선고 시 위법 판결 처리) |

이 구조 덕분에 새 사건을 추가할 때 이야기(대사)와 숫자(판정 기준)를 분리해서 작업할 수 있습니다.
→ React 재작성 시 `cases/*.json` 으로 분리한 이유이기도 합니다.

## 3. 코믹 사건을 넣은 이유

10개 사건 중 3개(공주 소동, 방귀 소동, 사극 낭독 소동)는 "애초에 죄가 성립하지 않는" 사건입니다.

- 진지한 사건만 있으면 플레이어가 "일단 무거운 형을 내리면 안전하다"는 휴리스틱에 빠지기 쉬움
- 코믹 사건을 섞어서 "이건 아예 무죄가 정답"인 경우를 학습시키고,
  판단 기준이 "형량의 크고 작음"이 아니라 **"이 행위가 실제로 범죄를 구성하는가"**임을
  자연스럽게 체감하게 만들었습니다.

## 4. 온보딩(튜토리얼) 설계

첫 플레이 진입장벽을 낮추기 위해:
- 아주 경미한 사건(무단횡단)으로 튜토리얼 구성
- 게임 시스템 설명을 나레이션 대사 안에 자연스럽게 녹여서 전달 (별도 팝업 최소화)
- 튜토리얼 결과는 기록/업적에 영향을 주지 않도록 별도 상태로 분리

## 5. 사운드를 전부 코드로 만든 이유

외부 음원 파일 라이선스 문제를 피하고, 파일 하나로 배포 가능한 구조를 유지하기 위해
Web Audio API 오실레이터로 모든 사운드(BGM, 블립음, 방청객 반응음, 문 여닫는 소리 등)를
직접 생성했습니다. 사건 성격(코믹/무거운 사건)에 따라 다른 음계·템포를 쓰도록 분기했습니다.

## 6. 앞으로의 방향

- **콘텐츠**: 사건 수를 늘릴수록 게임의 가치가 커지는 구조이므로, 우선순위 1순위
- **아키텍처**: 지금은 단일 HTML 파일(2,000줄+)이라 유지보수가 어려워지는 시점이 옴
  → `react-scaffold/`에서 데이터·로직·UI 분리 작업 시작
- **검증**: 판정 로직(`judge()`)에 대한 단위 테스트를 먼저 만들어서, 사건을 추가할 때마다
  기존 밸런스가 깨지지 않는지 자동으로 확인할 수 있게 함

## 7. 사용자 테스트 계획 (제안)

실제로 사람들에게 플레이시켜 보고 다음을 확인하면 좋습니다.

- [ ] 어떤 사건에서 가장 판단이 갈렸는가 (의견이 극단적으로 나뉘는 사건 = 좋은 신호)
- [ ] 튜토리얼만 보고 나머지 UI(증거/요약/설정)를 스스로 찾아 쓸 수 있었는가
- [ ] "법왜곡죄"로 게임오버 됐을 때, 왜 실패했는지 이해했는가 (이해 못 하면 피드백 설계 실패)
- [ ] 코믹 사건에서 웃었는가, 아니면 오히려 헷갈려했는가

간단한 구글 폼으로 5문항 정도만 받아도 다음 버전 방향을 잡는 데 충분합니다.
