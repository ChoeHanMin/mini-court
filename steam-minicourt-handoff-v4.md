# 미니법정 (Mini Court) — Steam 출시 인수인계 문서 v4

**이 문서는 `steamminicourthandoffv3.md`를 이어받아, 그 이후(현재 세션) 진행된 모든 작업을 반영한 최신본입니다.**
v3 문서의 0~14번 섹션 내용은 여전히 유효하며 아래에 이어서 15번부터 계속됩니다.

---

## 0-업데이트. 한눈에 보는 현재 상태 (v3 대비 변경분)

| 항목 | 상태 |
|---|---|
| 게임 버전 (index.html `GAME_VERSION`) | **1.16.0.0** (세션 시작 시점 1.14.0.0) |
| 최신 Steam Windows 빌드 zip | `MiniCourt_win32_x64_v1.16.0.0.zip` (약 121MB, 5분할 전송 완료) |
| 총 사건 수 | **65개** (기존 55개에서 10개 신규 추가) |
| GitHub 브랜치 | `claude/steam-game-auto-upload-yfbr71` (계속 이 브랜치에 커밋·푸시 중) |
| 최근 커밋 | `f98bdda` (v1.16.0.0), `b36db61` (v1.15.0.0) |
| Steamworks 로그인 계정 | tomjenis (v3와 동일) |

**중요**: 이번 세션에서 만든 최신 Windows 빌드(v1.16.0.0)는 **아직 사용자가 Steam에 업로드하는 중**이며, 대화 도중 PowerShell 명령어 오류 2건을 겪고 해결 방법을 안내받은 상태입니다 (아래 15번 섹션 참고). 다음 세션을 시작하는 사람은 **사용자가 실제로 `steamcmd.exe +login tomjenis +run_app_build ...`를 성공적으로 실행해서 새 BuildID가 나왔는지, 그리고 그 빌드를 default 브랜치에 Set Build Live 했는지**를 가장 먼저 확인해야 합니다.

---

## 15. 이번 세션 중 발생한 Steam 업로드 실전 트러블슈팅 (사용자 실수 + 해결)

세션 초반, 사용자가 로컬 PC에서 v1.14.0.0 빌드를 업로드하며 겪은 문제들 (게임 자체 버그가 아니라 Windows/PowerShell 사용법 문제):

1. **`steamcmd.exe : 용어가 cmdlet...으로 인식되지 않습니다`**
   → 원인: PowerShell은 현재 폴더의 실행파일을 `steamcmd.exe`처럼 그냥 치면 실행하지 않음. 반드시 `.\steamcmd.exe`처럼 `.\`를 붙여야 함.

2. **`App build file does not exist: ..\scripts\app_build_5113110.vdf`**
   → 원인 1: 이미 `C:\steamcmd`로 `cd` 한 상태에서 `..\scripts\...`라고 쳐서 한 단계 더 위로 올라가버림 → `scripts\app_build.vdf`로 수정.
   → 원인 2: 파일명을 `app_build_5113110.vdf`/`depot_build_5113110.vdf`로 착각함. **실제 파일명은 AppID 접미사 없이 그냥 `app_build.vdf`, `depot_build.vdf`임** (v3 문서 10-2 섹션에도 이미 명시되어 있었으나 재확인).

3. **빌드 업로드 성공 확인**: 이 문제들을 해결한 뒤 실제로 `steamcmd.exe +login tomjenis +run_app_build scripts\app_build.vdf +quit` 실행 성공 → "Build 25279801 is now live for players on the branch 'default'" 확인 (v1.14.0.0 기준. 이후 패치노트 게시함).

이후 v1.15.0.0, v1.16.0.0을 만들면서 다시 업로드를 시도하는 과정에서 **PowerShell 관련 신규 문제 2건**이 추가로 발생함 (v1.16.0.0 빌드를 사용자에게 전달한 직후):

4. **`copy /b file1+file2+... dest`가 PowerShell에서 안 됨**
   - 증상: `Copy-Item : 인수를 허용하는 위치 매개 변수를 찾을 수 없습니다`, `PositionalParameterNotFound` 에러.
   - 원인: PowerShell에서 `copy`는 cmd.exe의 `copy`가 아니라 **`Copy-Item` cmdlet의 별칭(alias)**임. `Copy-Item`은 `/b` 플래그도, `+`로 여러 파일을 이어붙이는 문법도 지원하지 않음.
   - 해결책 두 가지 제시함:
     - (권장) PowerShell 창에서 그대로: `cmd /c copy /b part00+part01+...+part04 결과파일.zip` — `cmd /c`로 cmd.exe에게 명령을 넘겨서 실행.
     - 또는 시작 메뉴에서 "cmd" 검색 → 명령 프롬프트를 새로 열어서 원래 `copy /b ...` 그대로 실행.
   - **다음에도 5분할 zip 병합 안내할 때는 처음부터 `cmd /c copy /b ...` 형태로 안내하는 게 나음** (사용자가 PowerShell을 기본으로 쓰는 경향이 있었음).

5. **`+run_app_build scripts\app_build.vdf +quit` 명령에서 `<계정명>` 플레이스홀더를 그대로 타이핑함**
   - 증상: `'<' 연산자는 나중에 사용하도록 예약되어 있습니다`, `ParserError: RedirectionNotSupported`.
   - 원인: 안내 메시지에 예시로 적은 `<계정명>`(꺾쇠괄호로 감싼 플레이스홀더 표기)을 사용자가 "여기에 실제 계정명을 넣으라"는 뜻인 줄 모르고 **글자 그대로** `.\steamcmd.exe +login <계정명> ...`를 입력함. PowerShell은 `<`/`>`를 파일 리다이렉션 기호로 해석하므로 즉시 파싱 에러.
   - 해결: 사용자의 실제 Steam 계정명(**tomjenis**)을 확인받아, 꺾쇠괄호 없이 완성된 명령어를 다시 통째로 제공함:
     ```powershell
     cd C:\steamcmd
     .\steamcmd.exe +login tomjenis +run_app_build scripts\app_build.vdf +quit
     ```
   - **교훈: 앞으로 이 사용자에게 명령어를 안내할 때는 플레이스홀더(`<...>`, `[...]` 등)를 절대 쓰지 말고, 이미 알고 있는 실제 값(계정명 tomjenis 등)을 바로 채워서 완성된 명령어만 줄 것.** PowerShell 사용에 익숙하지 않은 것으로 보임.

**세션 종료 시점 상태**: 사용자가 위 5번 문제 해결 명령어를 받은 직후 "그 다음은?"이라고 물어 향후 절차(압축 해제 → 실행 확인 → content 폴더 교체 → steamcmd 업로드 → Steamworks에서 default 브랜치 Set Live)를 안내받았음. **아직 실제로 `+run_app_build`가 성공했다는 확인(BuildID 출력 스크린샷 등)은 받지 못한 상태로 대화가 종료됨.** 다음 세션에서 가장 먼저 이 진행 상황을 물어볼 것.

---

## 16. 게임 콘텐츠 작업 이력 (v1.14.0.0 → v1.16.0.0, 시간순 상세)

이번 세션은 매우 많은 양의 콘텐츠/기능 작업을 진행함. Steam 재업로드 관점에서 중요한 것은 **"게임이 커밋마다 실제로 무엇이 달라졌는가"**이므로, 커밋 단위로 정리함.

### 16-1. (세션 초반, v1.14.0.0 자체에 포함된 작업 — 이미 완료된 상태로 세션 시작)
- 부가형(몰수/자격정지 등) 선택 화면(`#extrasPanel`) CSS 누락으로 체크박스·버튼이 한 줄에 다 뭉쳐 나오던 레이아웃 버그 수정.
- PC 와이드 화면(가로 900px 이상)에서 타이틀 좌/우 2단 레이아웃, 증거 사진을 캔버스로 직접 그리는 시스템 도입, CCTV도 이모지 대신 캔버스로 그린 감시카메라 화면으로 교체, 최후 진술 전용 배경음악(`final_statement.mp3`) 추가, 판결 버튼 그리드 재설계.
- 영어 모드에서 타이틀 버튼 줄바꿈으로 버전 정보가 가려지던 버그 수정 (`.title-right` 너비 480→620px 등 조정).
- 태극기: 처음엔 SVG로 직접 그려서 워터마크로 넣었으나 "잘 안 보인다"는 피드백 → `.title-logo-row` 안에 `.title-flag-wrap`으로 재배치 + `flagWave` 애니메이션. 이후 사용자가 실제 태극기 사진 파일을 줘서, SVG 대신 `taegukgi.png`(547×365)를 `<img>`로 교체 (`TAEGUKGI_SVG` 상수명은 유지한 채 내용만 `<img src="taegukgi.png">`로 교체).
- "게임에 나오는 사건 및 인물 이름은 전부 각색된 것" 허구 고지 문구를 게임 시작 전 한/영 병기로 추가.
- 최후 진술 음악이 판결 사유 화면까지 이어지도록 `setSceneBGM('verdict')` 호출 위치를 `proceedToVerdict()` → `handleVerdictClick()` → 최종적으로 `proceedToReason()`으로 세 차례에 걸쳐 점진적으로 늦춤.
- 증거 사진에 애니메이션(칼날의 핏방울이 흘러내리는 등) 도입 — `loopDrawOnCanvas(cv, drawFn)`: `requestAnimationFrame`으로 매 프레임 다시 그리되, 캔버스가 DOM에서 사라지면(`!document.body.contains(cv)`) 자동으로 멈추는 자기 종료형 루프. 별도 정리(cleanup) 로직 없이도 화면 전환 시 안전하게 멈춤을 Playwright로 검증(전환 직후·600ms 후 모두 draw 호출 수 고정됨을 확인).
- 검사 측 증거뿐 아니라 변호인 측 제출 증거에도 오염(위법/편향) 사유를 넣어달라는 요청 → `TAINT_REASONS`(검사 측: 영장 없는 압수·강압 신문 등)와 `DEF_TAINT_REASONS`(변호인 측: 짜맞춘 알리바이·매수된 증인·편집된 영상 등)를 분리하고, `taintReasonPoolFor(ev, submitterWho)`로 증거 유형과 안 맞는 사유(예: CCTV인데 "강압 신문 끝에 받은 자백")가 안 나오게 필터링.

### 16-2. 자체 발견·수정한 버그 (사용자가 직접 리포트하지 않았고, 테스트 중 스스로 찾아 고침)
- **증거 종류(kind) 오분류 버그 1**: `classifyEvidenceVisual`에서 CCTV `scene` 판정에 쓰려고 사건 제목/죄명까지 섞은 `hay` 변수를 실물 사진 종류(`kind`) 판정에도 그대로 써버려서, 예를 들어 "강간·방화 경합범" 사건의 무관한 DNA 증거가 사건 제목의 "방화" 때문에 불꽃 아이콘으로 잘못 나오는 문제 발견 → `kindHay`(증거 항목 자체 내용만)와 `hay`(kindHay+사건 맥락, scene 판정 전용)로 분리.
- **증거 종류 오분류 버그 2**: "흉기(식칼)에서 검출된 DNA"처럼 흉기 언급이 함께 있는 유전자 감정서가 칼 아이콘으로 먼저 매칭되던 문제 → DNA/혈흔 키워드가 있는 forensic 타입 증거는 흉기 판정보다 먼저 확인하는 우선순위 규칙 추가.
- **증거 종류 오분류 버그 3 (이번 세션)**: 새로 추가한 부검 감정서가, 본문(사인)에 "예기(칼)"라는 표현이 있어서 칼 사진으로 잘못 분류됨 → 증거 **이름/제목**만 보는 `nameTitleHay`를 새로 만들어 부검/지문/족적/계약서 판정을 본문 전체 검색보다 먼저 하도록 수정.
- **오염 증거 사유 불일치 버그**: CCTV 영상인데 "강압적 신문 끝에 받은 자백"처럼 말이 안 되는 사유가 붙던 문제 → 증거 유형별로 그럴듯한 사유만 고르도록 `taintReasonPoolFor` 로직 정교화.

### 16-3. v1.15.0.0 (커밋 `b36db61`) — 대규모 8항목 배치

사용자 요청 원문 요지: "①픽셀 더 작게(디테일↑) ②새 증거 종류 ③검사·변호인 논쟁 더 자주 ④신규 사건 10개(토막살인 등) ⑤법정 배경 디테일화(제공한 사진 활용) ⑥CCTV 밝기 다양화 ⑦증거 디테일 극대화 ⑧항소심 돌발상황+원심 증거채택 나레이션".

- **① 캐릭터 스프라이트 미세 디테일**: 기존 28×40 HD 그리드 해상도 자체는 건드리지 않고(안경/수갑/뱃지/표정 등 좌표 기반 옵션이 깨질 위험 회피), `drawSpriteFineDetail()`이라는 보조 레이어를 그 위에 얹음 — 머리카락 결(대각선 하이라이트), 옷감 주름(세로 음영 줄무늬), 피부 음영, 눈동자 하이라이트, 정장 단추 3개를 추가. 60가지 무작위 외모/표정 조합으로 스트레스 테스트하여 크래시 없음 확인.
- **② 신규 증거 종류 4개**: 지문(fingerprint), 족적(footprint, 감식용 노란 눈금자 포함하도록 재작업), 계약서(contract), 부검 감정서(autopsy) — 각각 전용 캔버스 드로잉 함수를 `EV_PHOTO_DRAWERS`에 추가.
- **③ 검사·변호인 즉석 재반박 시스템**: `REBUTTAL_TEMPLATES`(한/영 14쌍, 특정 사건 내용을 지칭하지 않는 범용 절차·증거·태도 공방)를 만들어, 매 라운드마다 `0.3 + (후반부 가산) + (격앙 가산)` 확률로 추가 반박 공방 한 쌍을 끼워 넣음. **여기서 심각한 잠재 버그를 발견·수정함** (아래 16-4 참고).
- **④ 신규 사건 10개 (id 56~65)**: 토막살인, 보험금 청부살인, 완전범죄를 꿈꾼 독살, 억울하게 누명 쓴 살인(족적 불일치로 무죄), 유산 노린 존속살해 의혹, 치정 삼각관계 살인, 동업자를 살해한 정산 다툼, 정당방위로 주장된 가정폭력 살인(무죄), 콜드케이스 15년 만의 DNA 재감정, 묻지마 살인. 전부 국영문 완비, `fairMin/fairMax/outrageLow` 등 양형 범위까지 세팅. `EXTREME_CASE_IDS`(사형/무기징역급이라 초심자 모드에서 제외되는 사건)에 56·57·58·60·64·65 추가. "전 사건 마스터" 업적을 하드코딩된 55 대신 `CASES.length`를 참조하도록 수정.
- **⑤ 법정 배경 디테일화**: 사용자가 "저번에 준 법정 사진"을 언급했으나, **업로드 폴더를 확인해도 실제 파일이 없었음** (이 시점엔 아직 안 줌). 부득이 대체 배경을 직접 새로 제작: SVG로 목재 패널·아치창(빛 번짐)·기둥·"저울(천칭)" 사법부 엠블럼을 그리고, `<feGaussianBlur>`로 은은한 블러를 SVG 내부에 미리 구워 넣어 "사진처럼 살짝 흐릿한" 느낌을 냄. 기존 픽셀아트 PNG 3종(default/comedy/heavy)을 이 SVG 하나로 교체하고 `image-rendering:pixelated`를 제거 (배경만 부드럽게, 전경 캐릭터는 그대로 픽셀 유지). **→ 이후 사용자가 실제 사진 4장을 줬으나(v1.16.0.0 시점), 그건 "메인 화면"용으로 쓰였고 이 인게임 재판 배경(`#scene`)은 아직 SVG 그대로임 — 다음 세션에서 실제 사진으로 교체 여지 있음 (아래 20번 참고).**
- **⑥ CCTV 주간/야간 밝기**: `d.timestamp`에서 `HH:MM` 패턴을 파싱해 06~19시는 'day', 그 외는 'night'로 판정(타임스탬프 없으면 "대낮/오전" 등 키워드로 폴백). `drawCctvFrame`에 `brightness` 매개변수를 추가해 주간엔 담백한 미색~회백색 톤, 야간엔 기존 초록 저조도 톤으로 배경/그레인/비네트 강도를 분기. "지금은 배경이 SF 초록색뿐"이라는 사용자 불만 해결.
- **⑦ 증거 디테일 극대화**: 위 ①②⑥에 이미 반영. 개별 드로어(칼의 핏방울 애니메이션, 혈흔 웅덩이 등)도 이미 이전 세션에 상세화되어 있었음.
- **⑧ 항소심 강화**: `APPEAL_SURPRISE_EVENTS`(거짓반성문·뒤늦은 공탁·구치소 징계 전력·면회 대화, 각 45% 확률로 사건당 1건) + `buildAppealEvidenceNarration(c)`(원심이 왜 그 증거를 채택/기각했는지 사유를 무작위로 붙여 나레이션, 사건당 최대 2건). 두 기능 모두 `buildAppealDialogueQueue`에 삽입.

### 16-4. v1.15.0.0 작업 중 발견한 심각한 잠재 버그 — 무한루프

`REBUTTAL_TEMPLATES`에서 아직 안 쓴 템플릿을 고르는 로직을 처음엔
```js
let idx = Math.floor(Math.random()*REBUTTAL_TEMPLATES.length);
while(usedRebuttals.has(idx)) idx = Math.floor(Math.random()*REBUTTAL_TEMPLATES.length);
```
로 짰는데, 기존 회귀 테스트 스크립트(`pw_outburst_full.js`)가 `Math.random`을 `() => 0.01`처럼 **고정값으로 모킹**해서 다른 무작위 분기를 결정적으로 만드는 방식으로 테스트하고 있었음. 이 경우 `idx`가 항상 같은 값으로 계산되어 `usedRebuttals.has(idx)`가 계속 참이면 **while문이 영원히 끝나지 않음** → 해당 테스트가 150초+ 동안 응답 없이 멈춤(hang)을 확인. 실제 게임 플레이에서 `Math.random()`이 진짜 무작위라면 거의 안 터지겠지만, **결정론적이지 않은 어떤 자바스크립트 엔진/환경에서도 이론적으로 재현 가능한 잠재적 프리징 버그**였음. 수정: while 재시도 대신, 아직 안 쓴 인덱스 목록을 배열로 만들어 그 중에서 직접 고르는 방식으로 교체 (`Math.random()`이 무엇을 반환하든 절대 무한루프에 빠지지 않음). 수정 후 `Math.random`을 고정값(0.01)으로 못박은 채 65개 사건 전체의 `buildDialogueQueue`를 돌려도 15초 이내에 전부 완료됨을 재확인.

### 16-5. v1.16.0.0 (커밋 `f98bdda`) — 4항목 배치

사용자가 이번엔 **실제 법정 사진 4장**(zip 파일로 첨부, 한글 파일명이 깨져서 `#Ubc95#Uc8151.png` 등으로 보였으나 압축 해제해서 확인함)을 처음으로 제공. 요청 4가지:

1. **집행유예 세부화** (형법 제62조·제62조의2 근거 명시): 기존엔 "집행유예" 버튼 클릭 시 아무 세부사항 없이 그냥 선고되던 것을, 실제 형법처럼 정교하게 만듦.
   - 새 화면 `#suspendedPanel`: ①기본형(6/8/10/12/18/24/30/36개월 중 선택) ②집행유예 기간(1~5년) ③보호관찰 부가 여부(토글) ④사회봉사명령(0/40/80/120/160/200/300시간) ⑤수강명령(0/40/80/100시간, `suggestedEducationProgram(c)`가 사건 죄명 키워드로 준법운전강의/성폭력 치료프로그램/마약류 재범방지교육/폭력예방강의/준법의식강의 중 자동 추천).
   - `verdictLabel()`을 확장해 `"징역 1년, 집행유예 2년 + 보호관찰 2년"`, `"징역 8개월, 집행유예 2년 + 사회봉사 120시간"`처럼 사용자가 제시한 예시 포맷 그대로 출력되도록 함 (한/영 모두 검증).
   - `proceedToExtras(type, years, presetExtras)`에 `presetExtras` 매개변수를 추가해, 집행유예 세부사항과 기존 몰수/자격정지·자격상실(사건별 부가형)이 서로 덮어쓰지 않고 병합되도록 함.
2. **메인화면(타이틀) 애니메이션 배경**: 받은 사진 4장을 1280px 폭 JPEG(quality 82)로 압축(각 117~144KB, 총 ~520KB)해서 `title-bg1.jpg`~`title-bg4.jpg`로 저장. `showTitle()`의 `ov.innerHTML`에 `.title-bg-cycle`(4장을 32초 주기로 서로 다른 딜레이를 줘서 교차 페이드) + `.title-bg-scrim`(하단이 더 어두운 그라디언트, 버튼/텍스트 가독성용) + Ken Burns 느낌의 느린 확대/이동(`titleBgKen` 키프레임)을 추가. 버튼은 기존 불투명 배경(`.btn`)을 그대로 유지해 요청("버튼 가독성은 높이되")대로 전혀 영향 없음. `prefers-reduced-motion`과 고대비 모드(`#stage.high-contrast`)에서는 애니메이션/배경을 끄도록 처리.
3. **재차 난동**: 첫 난동에서 "구두 경고"를 택한 경우에만, 사건당 최대 1회, 약 45% 확률로 나중에 `buildOutburstEscalation()`이 대사 큐 뒷부분에 동적으로 삽입됨. "이미 한 번 경고했음에도"를 명시하는 대사 + "퇴정명령+감치" 또는 "마지막 경고" 선택지.
4. **법원 경위 등장/퇴장**: `BAILIFF_PALETTE`(짙은 감색 제복)와 `drawSprite`에 새로 추가한 `opts.cap`(정모+챙+금색 배지)을 조합해 `drawBailiff()`로 그림. `#bailiffLeft`/`#bailiffRight` 두 캔버스를 피고인석 양옆에 배치, `.show` 클래스 토글로 opacity+transform 슬라이드 인/아웃(0.45초 트랜지션). 첫 난동·재차 난동 두 이벤트 모두에서 등장(narrator 라인에 `bailiffs:'show'`)/퇴장(`bailiffs:'hide'`) 트리거를 심음. 새 사건 시작 시(`randomizeAppearance()`)에도 확실히 숨겨지도록 방어 코드 추가.

### 16-6. v1.16.0.0 작업 중 발견·수정한 버그
- **`#suspendedPanel` CSS 누락 버그**: 새로 만든 집행유예 세부 패널에 전용 CSS 규칙을 안 넣어서, JS가 `display:flex`로 바꾸는 순간 기본값인 `flex-direction:row`가 적용되어 "뒤로"/"선고 확정" 버튼이 화면 오른쪽에 거대한 세로 막대로 어긋나 보이는 문제 발생. **이건 세션 초반에 `#extrasPanel`에서 이미 한 번 겪었던 것과 완전히 같은 유형의 버그**(과거 v3 이전 세션 기록에도 남아있음) — `#suspendedPanel{display:none;flex-direction:column;align-items:center;width:100%;margin-top:6px;}`로 동일 패턴 적용해 수정. **교훈: 이 프로젝트에서 새 패널을 만들 때마다 반드시 전용 CSS의 `flex-direction:column`을 같이 넣을 것 (잊기 쉬운 함정).**

---

## 17. 이번 세션의 Electron 빌드 재생성 절차 (v3의 10-1과 다른 점)

v3 문서(10-1)는 **처음부터 Electron 프로젝트를 새로 구성**하는 절차(wine64 필요, rcedit 등)를 설명하지만, 이번 세션에서는 **이미 만들어져 있던 `$SCRATCHPAD/electron-build/app/` 폴더를 재사용**했으므로 훨씬 단순했음. 이 폴더는 세션(컨테이너) 간에 유지되지 않을 수 있으므로, 다음 세션에서 이 폴더가 없다면 v3의 10-1 절차대로 처음부터 새로 구성해야 함.

**이번 세션에 실제로 한 절차** (폴더가 이미 있다는 전제):
1. `cp index.html $SCRATCHPAD/electron-build/app/index.html` (그리고 이번처럼 새 바이너리 자산이 있으면 같이 복사 — 이번엔 `title-bg1~4.jpg` 4개를 추가로 복사함)
2. `md5sum`으로 원본과 복사본 index.html이 100% 일치하는지 확인
3. `sed -i 's/"version": "구버전"/"version": "신버전"/' package.json`
4. `rm -rf dist/MiniCourt-win32-x64 && npx electron-packager . MiniCourt --platform=win32 --arch=x64 --out=dist --icon=icon.ico --overwrite`
5. 패키징 후 `resources/app/` 안의 `index.html`과 새로 추가한 자산들의 md5가 원본과 일치하는지 재검증
6. `zip -r -q -X 결과.zip MiniCourt-win32-x64` (폴더째로 압축)
7. `split -b 25M -d -a 2 결과.zip 결과.zip_part` → 파일명을 `_part`에서 `.part`로 변경(`part00~part04`, 언더바 없이 점(`.`) 앞에 표기하는 게 관례로 자리잡음)
8. 별도 임시 폴더에서 5개 파트를 다시 이어붙여 md5가 원본 zip과 일치하는지 재검증 (이번 세션 두 버전 모두 100% 일치 확인됨)
9. `SendUserFile`로 5개 파트 전달 + PowerShell 병합 명령어(`cmd /c copy /b ...` 형태 권장, 15번 섹션 참고) + 최종 zip의 MD5 해시값 안내

이번 세션에서는 electron-packager 실행 시 **wine64 관련 에러가 전혀 발생하지 않았음** (v3에서 언급된 함정과 달리, 이번 컨테이너 환경엔 이미 필요한 게 갖춰져 있었던 것으로 보임). 또한 `resources/app/node_modules` 삭제 작업도 하지 않았는데, 두 버전 모두 zip 크기가 121MB 안팎으로 기존 버전들과 비슷해 특별히 비대해지는 문제는 없었음 (v3가 우려했던 "같은 폴더에 이전 zip이 남아있어서 재귀적으로 포함되는" 문제는, 매번 `rm -rf dist/MiniCourt-win32-x64`로 지우고 시작했고 zip 결과물 자체는 `electron-build/` 최상위에 저장해 `app/` 폴더 밖에 있었기 때문에 재발하지 않음).

---

## 18. 패치노트 발행 이력

대화 중 사용자가 "패치노트 좀"이라고 요청할 때마다, **지난 패치노트 요청 이후 누적된 변경사항**을 한/영 병기로 정리해 Steam 게시판용 텍스트로 제공하는 패턴을 반복함. 가장 최근에 제공한 것은 v1.15.0.0+v1.16.0.0 통합 패치노트 (2026-09-13 기준, 아래 요지):

- v1.15.0.0: 신규 사건 10건, 신규 증거 종류(지문/족적/계약서/부검), CCTV 주야 구분, 재반박 시스템, 항소심 강화 4종, 캐릭터 미세 디테일, 법정 배경 일러스트화.
- v1.16.0.0: 집행유예 세부 선고, 메인화면 애니메이션 배경, 재차 난동, 법원 경위 등장.

전문은 이 대화의 채팅 로그에 그대로 남아있으므로, 다음 세션에서 Steam 게시판에 아직 안 올렸다면 그 텍스트를 그대로 재사용하면 됨.

---

## 19. 게임 자산 파일 목록 (`/home/user/mini-court/` 저장소 기준, 이번 세션에 추가/변경된 것)

| 파일 | 설명 | 비고 |
|---|---|---|
| `index.html` | 게임 본체 (단일 파일) | GAME_VERSION 1.16.0.0 |
| `taegukgi.png` | 실제 태극기 이미지 (547×365) | v3 이전 세션에 추가됨, 변경 없음 |
| `title-bg1.jpg` ~ `title-bg4.jpg` | 메인화면 배경용 실제 법정 사진 4장 (각 1280px 폭 JPEG, 117~144KB) | **이번 세션 신규 추가**. 사용자가 원본 PNG(1672×941, 각 1.7MB)를 zip으로 제공 → 압축해서 저장 |
| (Electron 빌드 폴더 내부에만 존재, git 미추적) `final_statement.mp3`, `trial_theme.mp3`, `tense_theme.mp3`, `theme.mp3`, `result_good.mp3`, `result_bad.mp3`, `click_sfx.mp3` | 배경음악/효과음 | v3 이전부터 존재. Electron 빌드 시 함께 패키징됨 |
| `courtroom_bg.svg`의 내용 (파일로는 존재하지 않고 index.html의 CSS `background` 안에 URL 인코딩된 데이터 URI로 직접 박혀있음) | 인게임 재판 장면(`#scene`) 뒷배경 | 실제 사진이 없어서 대체 제작한 벡터 일러스트. **다음 세션에서 title-bg 사진들을 재활용해 실제 사진 기반으로 교체할 수 있음 (아래 20번 참고)** |

---

## 20. 다음 세션에서 이어서 할 만한 일 (미완료/개선 여지)

1. **v1.16.0.0 Steam 업로드 완료 확인** (최우선) — 15번 섹션 참고. `steamcmd` 성공 여부, BuildID, default 브랜치 Set Live 여부를 사용자에게 먼저 물어볼 것.
2. **인게임 재판 배경(`#scene`)을 실제 법정 사진으로 교체**: 현재는 벡터 일러스트(SVG)로 대체되어 있음. 이제 사용자가 준 `title-bg1~4.jpg`(또는 그 원본 고화질 PNG)가 있으니, 이 중 하나를 살짝 흐릿하게 처리해 `#scene`의 세 가지 배경 규칙(`#scene`, `.scene-comedy`, `.scene-heavy`)에 있는 SVG data URI를 실제 사진으로 바꿔치기하면 사용자의 원래 요청("법정 사진을 약간 흐릿하게 해서 넣자")을 완전히 충족시킬 수 있음. 다만 파일 크기(사진 1장당 ~120~140KB)가 SVG(약 14KB 텍스트)보다 커지므로, 게임 파일 용량에 미치는 영향을 고려해서 진행할 것.
3. **스프라이트 해상도 자체 업그레이드**: 이번 세션엔 리스크 회피 차원에서 28×40 그리드 위에 "보조 디테일 레이어"만 얹었음. 사용자가 "픽셀을 더 작게"를 문자 그대로(그리드 해상도 자체를 2배 더 올리는 것) 원했다면, 안경/수갑/뱃지/표정 등 좌표 기반 옵션 전체를 좌표 변환(각 좌표를 2배로 스케일)해야 하는 큰 리팩터링이 필요함 — 아직 안 함.
4. **패치노트를 실제로 Steam 커뮤니티 허브에 게시했는지 확인**: 지금까지는 텍스트를 만들어서 전달만 했고, 실제 게시 작업은 사용자가 직접 Steamworks에서 진행하는 것으로 보임.
5. **CLAUDE.md 등에 Steam 계정명(tomjenis) 영구 기록 여부**: 사용자가 "tomjenis인거 외워"라고 했을 때, 세션 간 영구 기억이 안 된다는 점을 안내하고 `CLAUDE.md` 저장 여부를 물어봤으나 아직 답을 못 받음. 다음 세션에서 필요하면 다시 확인.

---

## 21. 기타 (v1~v3에서 이어지는 내용 중 이번 세션에도 재확인된 것)

- App ID 5113110, Depot ID 5113111, 계정명 tomjenis — 변경 없음.
- 이번 세션 내내 모든 작업은 `Playwright`(헤드리스 Chromium) 기반 자동 테스트로 매 기능마다 스크린샷/동작 검증을 거친 뒤에만 커밋·배포함 (회귀 스크립트: `pw_regression.js`, `pw_all_evidence_check(_en).js`, `pw_outburst_full.js`, `pw_outburst_test.js`, `pw_sentencing_announce.js`, `pw_clicksfx_scope.js` 등을 세션 내내 재사용/확장함).
- 커밋 메시지와 PR은 항상 한/영 요약을 포함하고, `claude/steam-game-auto-upload-yfbr71` 브랜치에 직접 push (PR은 별도로 만들지 않음, 사용자가 명시적으로 요청한 적 없음).
